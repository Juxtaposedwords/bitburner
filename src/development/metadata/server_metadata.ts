// AUTO-GENERATED BY PROTO-GENERATOR - DO NOT EDIT
import { NS } from "@ns";
import * as rpc from "development/libraries/rpc";
import { Codes } from "development/libraries/status";

/** Port this service listens on, assigned by the generator's port registry. */
export const SupervisorServicePort = 10;

export enum ServerStatus {
    DISCOVERED = 0,
    ROOTABLE = 1,
    ROOTED = 2,
    ELIGIBLE = 3,
}

export interface UpdateMetadataRequest {
    server?: Metadata;
}

export interface UpdateMetadataResponse {
}

export interface PatchMetadataRequest {
    server?: Metadata;
}

export interface PatchMetadataResponse {
}

export interface ListServersRequest {
    eligibleOnly?: boolean;
}

export interface ListServersResponse {
    servers?: Metadata[];
}

export interface Metadata {
    hostname?: string;
    organization?: string;
    ip?: string;
    pathFromHome?: string;
    maxRam?: number;
    ramAvailable?: number;
    cpuCores?: number;
    purchasedByPlayer?: boolean;
    securityLevel?: number;
    minSecurityLevel?: number;
    growthMultiplier?: number;
    backdoorInstalled?: boolean;
    hacking?: HackingData;
    moneyAvailable?: number;
    maxMoney?: number;
    status?: ServerStatus;
}

export interface HackingData {
    requirements?: HackingRequirements;
    ports?: HackingPorts;
}

export interface HackingRequirements {
    level?: number;
    ports?: number;
}

export interface HackingPorts {
    ssh?: boolean;
    ftp?: boolean;
    smtp?: boolean;
    http?: boolean;
    sql?: boolean;
}

export interface SupervisorServiceHandlers {
    UpdateMetadata: (req: UpdateMetadataRequest) => Promise<UpdateMetadataResponse> | UpdateMetadataResponse;
    PatchMetadata: (req: PatchMetadataRequest) => Promise<PatchMetadataResponse> | PatchMetadataResponse;
    ListServers: (req: ListServersRequest) => Promise<ListServersResponse> | ListServersResponse;
}

export function NewSupervisorServiceClient(ns: NS, targetPort = SupervisorServicePort) {
    return {
        UpdateMetadata: async (req: UpdateMetadataRequest, timeoutMs = 10000): Promise<rpc.RpcResponse<UpdateMetadataResponse>> => {
            const replyPort = rpc.nextReplyPort(ns);
            ns.clearPort(replyPort);
            const envelope: rpc.RpcEnvelope<UpdateMetadataRequest> = { service: "SupervisorService", method: "UpdateMetadata", replyPort, payload: req };

            const deadline = Date.now() + timeoutMs;

            // Ports hold a bounded number of entries; writePort silently evicts on overflow.
            const queued = await rpc.pollWithBackoff(ns, () => ns.tryWritePort(targetPort, JSON.stringify(envelope)), deadline);
            if (!queued) {
                return { status: Codes.DEADLINE_EXCEEDED, error: `Call to SupervisorService.UpdateMetadata could not be queued.` };
            }

            const replied = await rpc.pollWithBackoff(ns, () => ns.peek(replyPort) !== "NULL PORT DATA", deadline);
            if (!replied) {
                return { status: Codes.DEADLINE_EXCEEDED, error: `Call to SupervisorService.UpdateMetadata timed out.` };
            }

            // The server replies with JSON.stringify(response); parse it back.
            return JSON.parse(ns.readPort(replyPort) as string) as rpc.RpcResponse<UpdateMetadataResponse>;
        },
        PatchMetadata: async (req: PatchMetadataRequest, timeoutMs = 10000): Promise<rpc.RpcResponse<PatchMetadataResponse>> => {
            const replyPort = rpc.nextReplyPort(ns);
            ns.clearPort(replyPort);
            const envelope: rpc.RpcEnvelope<PatchMetadataRequest> = { service: "SupervisorService", method: "PatchMetadata", replyPort, payload: req };

            const deadline = Date.now() + timeoutMs;

            // Ports hold a bounded number of entries; writePort silently evicts on overflow.
            const queued = await rpc.pollWithBackoff(ns, () => ns.tryWritePort(targetPort, JSON.stringify(envelope)), deadline);
            if (!queued) {
                return { status: Codes.DEADLINE_EXCEEDED, error: `Call to SupervisorService.PatchMetadata could not be queued.` };
            }

            const replied = await rpc.pollWithBackoff(ns, () => ns.peek(replyPort) !== "NULL PORT DATA", deadline);
            if (!replied) {
                return { status: Codes.DEADLINE_EXCEEDED, error: `Call to SupervisorService.PatchMetadata timed out.` };
            }

            // The server replies with JSON.stringify(response); parse it back.
            return JSON.parse(ns.readPort(replyPort) as string) as rpc.RpcResponse<PatchMetadataResponse>;
        },
        ListServers: async (req: ListServersRequest, timeoutMs = 10000): Promise<rpc.RpcResponse<ListServersResponse>> => {
            const replyPort = rpc.nextReplyPort(ns);
            ns.clearPort(replyPort);
            const envelope: rpc.RpcEnvelope<ListServersRequest> = { service: "SupervisorService", method: "ListServers", replyPort, payload: req };

            const deadline = Date.now() + timeoutMs;

            // Ports hold a bounded number of entries; writePort silently evicts on overflow.
            const queued = await rpc.pollWithBackoff(ns, () => ns.tryWritePort(targetPort, JSON.stringify(envelope)), deadline);
            if (!queued) {
                return { status: Codes.DEADLINE_EXCEEDED, error: `Call to SupervisorService.ListServers could not be queued.` };
            }

            const replied = await rpc.pollWithBackoff(ns, () => ns.peek(replyPort) !== "NULL PORT DATA", deadline);
            if (!replied) {
                return { status: Codes.DEADLINE_EXCEEDED, error: `Call to SupervisorService.ListServers timed out.` };
            }

            // The server replies with JSON.stringify(response); parse it back.
            return JSON.parse(ns.readPort(replyPort) as string) as rpc.RpcResponse<ListServersResponse>;
        },
    };
}

export function RegisterSupervisorService(server: rpc.RpcServer, handlers: SupervisorServiceHandlers) {
    server.registerService("SupervisorService", handlers as unknown as Record<string, Function>);
}

